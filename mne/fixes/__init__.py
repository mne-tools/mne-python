from . fixes import (gzip_open, Counter, unique, bincount, copysign, in1d,
                     digitize, normalize_colors,
                     tril_indices, unravel_index, qr_economic, safe_copy,
                     filtfilt, firwin2, matrix_rank, partial, assert_true,
                     assert_is, assert_is_not, sparse_block_diag)
from . arraypad import pad
