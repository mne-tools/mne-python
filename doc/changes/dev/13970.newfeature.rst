Added public :class:`mne.viz.LayeredMesh` class (previously private ``_LayeredMesh``) with a :attr:`~mne.viz.LayeredMesh.smooth_mat` attribute for efficient real-time source-space data streaming. Added :attr:`mne.viz.Brain.layered_meshes` property to access meshes after plotting, and a ``key`` parameter to :meth:`mne.viz.Brain.add_data` for named overlays. Changes by `Payam Sadeghi-Shabestari`_.
