Fix bug where :func:`mne.filter.notch_filter` and :meth:`mne.io.Raw.notch_filter` with ``method="spectrum_fit"`` failed to detect and remove line noise whose frequency fell between FFT bins, and subtracted a beating artifact from neighboring bins, by `Eric Larson`_.
